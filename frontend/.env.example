# Copiar a .env.local (desarrollo) o inyectar en CI (build)
# Vacío (default) = mismo origen → requiere proxy (vite dev) o reverse proxy (producción).
# Con valor = origen absoluto de la API, p.ej. https://api.ejemplo.com
VITE_API_URL=
